node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Secrets — never commit. Only the template is tracked.
.env*
!.env.example

# Scratch build output from the production-readiness audit.
# Safe to delete these directories entirely.
.apirun/
.apitest/
.apitest2/
preview-build/

# Leftover scratch file from the audit (sandbox could not delete it).
src/__eb_test.tsx
.diag*/
.diag-ui.html
